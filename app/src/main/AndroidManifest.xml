<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.thesisguidance">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.lecturer.LecturerThesisDetailActivity"></activity>
        <activity android:name=".ui.student.AddThesisActivity" />
        <activity android:name=".ui.lecturer.LecturerGuidanceActivity" />
        <activity android:name=".ui.student.GuidanceDetailActivity" />
        <activity android:name=".ui.student.AddGuidanceActivity" />
        <activity android:name=".ui.student.GuidanceActivity" />
        <activity android:name=".ui.lecturer.LecturerLoginActivity" />
        <activity android:name=".ui.lecturer.LecturerGuidanceDetailActivity" />
        <activity android:name=".ui.student.StudentLoginActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>